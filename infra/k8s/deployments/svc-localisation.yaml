apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-localisation
  namespace: flotte-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc-localisation
  template:
    metadata:
      labels:
        app: svc-localisation
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3004"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: svc-localisation
        image: svc-localisation:dev
        imagePullPolicy: Never
        ports:
        - containerPort: 3004
        - containerPort: 50051
        env:
        - name: PORT
          value: "3004"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector-opentelemetry-collector:4317"
        - name: OTEL_SERVICE_NAME
          value: "svc-localisation"
        resources:
          limits:
            memory: 256Mi
            cpu: "250m"
          requests:
            memory: 128Mi
            cpu: "100m"
