apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-evenements
  namespace: flotte-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc-evenements
  template:
    metadata:
      labels:
        app: svc-evenements
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3003"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: svc-evenements
        image: svc-evenements:dev
        imagePullPolicy: Never
        ports:
        - containerPort: 3003
        env:
        - name: PORT
          value: "3003"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector-opentelemetry-collector:4317"
        - name: OTEL_SERVICE_NAME
          value: "svc-evenements"
        resources:
          limits:
            memory: 256Mi
            cpu: "250m"
          requests:
            memory: 128Mi
            cpu: "100m"
