apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-maintenance
  namespace: flotte-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc-maintenance
  template:
    metadata:
      labels:
        app: svc-maintenance
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3005"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: svc-maintenance
        image: svc-maintenance:dev
        imagePullPolicy: Never
        ports:
        - containerPort: 3005
        env:
        - name: PORT
          value: "3005"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector-opentelemetry-collector:4317"
        - name: OTEL_SERVICE_NAME
          value: "svc-maintenance"
        resources:
          limits:
            memory: 256Mi
            cpu: "250m"
          requests:
            memory: 128Mi
            cpu: "100m"
